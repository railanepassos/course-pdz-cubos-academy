/**
 * Infinity e NaN
 */

let x = 10, y = 0;

// Resultado é infinito
resultado = x / y;
console.log(`A dividão de ${x} e ${y} é ${resultado}`);

// Não é um numero
x = 0;
resultado = x / y;
console.log(`A dividão de ${x} e ${y} é ${resultado}`);

