/**
 * Resumo
 *  Arrays       ->  Permitem o armazenamento ordenado de dados semelhantes;
 *  index        ->  Posição de cada item na lista, começando sempre em zero;
 *                  O ultimo item da lista tem sempre index = length -1
 * 
 * Métodos úteis ao trabalhar com Arrays
 *  .push()      ->  adiciona item ao fim da lista
 *  .pop()       ->  remove um item do fim da lista
 *  .inshift()   ->  adiciona item ao inicio da lista
 *  .shift()     ->  remove um item do início da lista
 * 
 */


const nome = ['Railane', 'Davi', 'Sindel', 'Bob'];

console.log(nome);
console.log(nome[3]);
console.log(nome[4]);