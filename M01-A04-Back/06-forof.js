const nomes = ['Railane', 'Davi', 'Sindel', 'Bob'];

/**
 * for(let varriavel of array) ->   Comum quando se quer passar por todos os itens de um array
 *                                  Enquanto a condição for verdadeira executa uma ação
 */
for (let nome of nomes) {
    console.log(nome);
    console.log("Proximo passo do loop ---> ");
}