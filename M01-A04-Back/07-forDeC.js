/**
 * nomes.forEach(element => {
 * console.log('Numero gerado -->');
 * console.log(element);
 * });
 */

/**
 * 
 * let i = 1;
 * while (i<=10) {
 * console.log(i);
 * i++
 * }
 *  
 */

const nomes = ['Railane', 'Davi', 'Sindel', 'Bob'];

for (let index = 0; index < nomes.length; index++) {
    const element = nomes[index];
    console.log(element);
}

